# Online Play Settings
online_play:
  # Target website to play against (e.g., 'https://quoridor.sambaldwin.dev/')
  target_website: "https://quoridor.sambaldwin.dev/"
  
  # Account settings
  mode: "guest" # Options: 'guest', 'account'
  credentials:
    username: ""
    password: ""
  
  # Bot behavior
  games_to_play: 4                 # Total number of games to play this session
  time_limit_per_move_ms: 2000
  max_parallel_games: 2            # How many games to run simultaneously (set to 1 to disable parallelism)

  # Set this to a directory to record webm videos of the games
  record_video_dir: "/content/drive/MyDrive/quoridor_videos/"

# Model Management
models:
  # Directory to save models to
  checkpoint_dir: "/content/drive/MyDrive/quoridor_models/"
  
  # Checkpointing settings
  save_interval: 100        # Save a regular checkpoint every N games
  super_save_interval: 1000 # Save a permanent "super" checkpoint every M games
  
  # The model to load and play with (will default to latest if empty or 'latest')
  active_model: "latest"
  
  # List of models to delete from storage (to save space)
  delete_models:
    - "quoridor_net_old.pt"

# MCTS (AlphaZero search) settings
mcts:
  simulations: 100   # Higher = stronger play but slower per move
